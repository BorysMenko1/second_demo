pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Get Target Branch') {
            steps {
                script {
                    def targetBranch = env.CHANGE_TARGET
                    echo "Target branch is: ${targetBranch}"
                }
            }
        }
    }
}
